{
  "name": "frogs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development bankai start -p 8080",
    "build": "npm run build-icons && npm run build-app && npm run build-staticfix",
    "build-app": "NODE_ENV=production bankai build",
    "build-staticfix": "mkdir -p ./dist && rm -f ./dist/200.html && ln -s index.html ./dist/200.html && rm -f ./dist/manifest.json && cp ./manifest.json ./dist/manifest.json && rm -f ./dist/favicon.ico && cp ./favicon.ico ./dist/favicon.ico && rm -f ./dist/CNAME && cp ./CNAME ./dist/CNAME && rm -f ./dist/.surgeignore && cp ./.surgeignore ./dist/.surgeignore",
    "build-icons": "svgo -f assets",
    "inspect": "bankai inspect",
    "test": "standard",
    "publish": "surge -p ./dist && git push",
    "publish-test": "npm run test && npm run build && surge -p ./dist -d frogs-test.surge.sh"
  },
  "author": "srilq <git@srilq.email>",
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-transform-es2015-arrow-functions": "^6.22.0",
    "babelify": "^7.3.0",
    "bankai": "^8.1.1",
    "brfs": "^1.4.3",
    "standard": "^10.0.3",
    "svgo": "^0.7.2"
  },
  "dependencies": {
    "choo": "^6.0.1",
    "choo-devtools": "^1.3.1",
    "choo-log": "^7.2.1",
    "choo-service-worker": "^2.3.0",
    "nanoanimation": "^1.0.0",
    "random-frog": "github:srilq/random-frog",
    "tachyons": "^4.8.1",
    "web-animations-js": "^2.3.1"
  },
  "browserify": {
    "transform": [
      "brfs",
      "babelify"
    ]
  },
  "babel": {
    "plugins": [
      "transform-es2015-arrow-functions"
    ]
  }
}
